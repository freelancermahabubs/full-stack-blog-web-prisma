import PlainLayout from "@/components/Master/Plain-Layout";
const Layout = ({children}) => {
    return (
        <PlainLayout>
            {children}
        </PlainLayout>
    );
};
export default Layout;